# Local development overrides — NOT for competition submission.
# docker-compose automatically merges this with docker-compose.yml.
# Remove or ignore this file when measuring memory compliance.

services:
  store:
    deploy:
      resources:
        limits:
          cpus: "0.55"
          memory: "512MB"
